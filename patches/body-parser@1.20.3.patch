[1mdiff --git a/lib/read.js b/lib/read.js[m
[1mindex fce6283f50961e68c2f576031ed5e3d4fdc39984..b3bd2525105a1572302cae3d53b0c35115c9a6fb 100644[m
[1m--- a/lib/read.js[m
[1m+++ b/lib/read.js[m
[36m@@ -14,11 +14,14 @@[m
 var createError = require('http-errors')[m
 var destroy = require('destroy')[m
 var getBody = require('raw-body')[m
[31m-var iconv = require('iconv-lite')[m
 var onFinished = require('on-finished')[m
 var unpipe = require('unpipe')[m
 var zlib = require('zlib')[m
 [m
[32m+[m[32mfunction getIconv () {[m
[32m+[m[32m  return require('iconv-lite')[m
[32m+[m[32m}[m
[32m+[m
 /**[m
  * Module exports.[m
  */[m
[36m@@ -67,7 +70,7 @@[m [mfunction read (req, res, next, parse, debug, options) {[m
     : encoding[m
 [m
   // assert charset is supported[m
[31m-  if (opts.encoding === null && encoding !== null && !iconv.encodingExists(encoding)) {[m
[32m+[m[32m  if (opts.encoding === null && encoding !== null && !getIconv().encodingExists(encoding)) {[m
     return next(createError(415, 'unsupported charset "' + encoding.toUpperCase() + '"', {[m
       charset: encoding.toLowerCase(),[m
       type: 'charset.unsupported'[m
[36m@@ -123,7 +126,7 @@[m [mfunction read (req, res, next, parse, debug, options) {[m
     try {[m
       debug('parse body')[m
       str = typeof body !== 'string' && encoding !== null[m
[31m-        ? iconv.decode(body, encoding)[m
[32m+[m[32m        ? getIconv().decode(body, encoding)[m
         : body[m
       req.body = parse(str)[m
     } catch (err) {[m
