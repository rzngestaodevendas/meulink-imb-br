import { trpc } from "@/lib/trpc";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Building2, Check, ExternalLink, Plus } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";

type Organization = { id: number; slug: string; name: string; publicName: string | null; createdAt: Date };

export default function Organizations() {
  const [name, setName] = useState("");
  const [publicName, setPublicName] = useState("");
  const organizations = trpc.organizations.list.useQuery();
  const utils = trpc.useUtils();
  const create = trpc.organizations.create.useMutation({ onSuccess: () => { toast.success("Construtora cadastrada e selecionada"); setName(""); setPublicName(""); utils.organizations.list.invalidate(); window.location.href = "/app"; }, onError: error => toast.error(error.message) });
  const select = trpc.organizations.select.useMutation({ onSuccess: () => { toast.success("Construtora selecionada"); window.location.href = "/app"; }, onError: error => toast.error(error.message) });

  return <div className="min-h-screen bg-[#f7f8fa]"><header className="mb-6 rounded-3xl bg-[#102c3d] px-6 py-7 text-white shadow-xl sm:px-8"><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-white/10"><Building2 className="h-5 w-5 text-[#d7b874]" /></div><div><div className="mb-2 text-xs font-bold uppercase tracking-[0.2em] text-[#d7b874]">MeuLink · operação</div><h1 className="text-2xl font-semibold tracking-tight sm:text-3xl">Construtoras</h1><p className="mt-2 text-sm text-white/70">Separe estoques, equipes e links por empresa.</p></div></div></header><div className="grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><Card className="border-0 shadow-sm"><CardHeader><CardTitle className="flex items-center gap-2 text-base text-[#102c3d]"><Plus className="h-4 w-4" /> Nova construtora</CardTitle><p className="text-xs text-slate-500">Você será administrador e poderá importar a tabela de imóveis em seguida.</p></CardHeader><CardContent><form onSubmit={event => { event.preventDefault(); create.mutate({ name, publicName }); }} className="grid gap-4"><label className="grid gap-2 text-sm font-medium">Nome empresarial<Input required value={name} onChange={event => setName(event.target.value)} placeholder="Construtora Horizonte" /></label><label className="grid gap-2 text-sm font-medium">Nome público<Input value={publicName} onChange={event => setPublicName(event.target.value)} placeholder="Horizonte Imóveis" /></label><Button type="submit" disabled={create.isPending} className="gap-2 bg-[#20bd63] hover:bg-[#12934a]"><Plus className="h-4 w-4" /> {create.isPending ? "Cadastrando..." : "Cadastrar construtora"}</Button></form></CardContent></Card><Card className="border-0 shadow-sm"><CardHeader><CardTitle className="text-base text-[#102c3d]">Empresas cadastradas</CardTitle><p className="text-xs text-slate-500">A seleção define em qual estoque os imóveis e links serão criados.</p></CardHeader><CardContent className="grid gap-3">{organizations.isLoading && <p className="py-6 text-center text-sm text-slate-500">Carregando empresas...</p>}{organizations.error && <p className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{organizations.error.message}</p>}{(organizations.data as Organization[] | undefined)?.map(organization => <div key={organization.id} className="rounded-xl border border-slate-200 p-4"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-semibold text-[#102c3d]">{organization.publicName || organization.name}</p><p className="text-xs text-slate-500">{organization.name}</p></div><div className="flex flex-wrap gap-2"><Button variant="outline" size="sm" onClick={() => select.mutate({ organizationId: organization.id })} className="gap-2"><Check className="h-4 w-4" /> Selecionar</Button><a href={`/tabela/${organization.slug}`} target="_blank" rel="noreferrer" className="inline-flex h-9 items-center gap-2 rounded-md border border-slate-200 px-3 text-sm font-medium text-[#102c3d] hover:bg-slate-50"><ExternalLink className="h-4 w-4" /> Portal da tabela</a></div></div><p className="mt-3 break-all rounded-lg bg-slate-50 px-3 py-2 text-[11px] text-slate-500">{window.location.origin}/tabela/{organization.slug}</p></div>)}{!organizations.isLoading && !organizations.error && organizations.data?.length === 0 && <p className="py-6 text-center text-sm text-slate-500">Nenhuma construtora cadastrada.</p>}</CardContent></Card></div></div>;
}
